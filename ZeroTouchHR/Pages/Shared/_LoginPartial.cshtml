@using Microsoft.AspNetCore.Identity
@using Amazon.Extensions.CognitoAuthentication

@inject SignInManager<CognitoUser> SignInManager
@inject UserManager<CognitoUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <form asp-area="" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/Index", new { area = "" })" method="post" id="logoutForm" class="navbar-right">
        @*<ul class="nav navbar-nav navbar-right">
            <li>
                <a asp-area="" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
            </li>
            <li>
                <button type="submit" class="btn btn-link navbar-btn navbar-link">Logout</button>
            </li>
        </ul>*@
        <ul class="navbar-nav flex-grow-1">
            <li class="nav-item">
                <a class="nav-link  text-light" asp-page="/EmployeeList/Index">Dashboard</a>
            </li>

            <li class="nav-item mr-5">
                <a class="nav-link  text-light" asp-page="/EmployeeList/Create" title="Register">Create Employee</a>
            </li>
            <li class="nav-item mr-5">
                &nbsp;
            </li>
            <li class="nav-item mr-5">
                &nbsp;
            </li>
            <li class="nav-item mr-5">
                &nbsp;
            </li>

            <li class="nav-item ml-5">
                <a class="nav-link  text-light" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
            </li>

            <li class="nav-item">
                <button type="submit" class="btn btn-link navbar-btn navbar-link text-light">Logout</button>
            </li>
        </ul>
    </form>
}
else
{

<ul class="nav justify-content-end w-100">
    
    <li class="nav-item">
        <a class="nav-link text-light" asp-page="/Account/Login" title="Login">Login</a>
    </li>
</ul>
}

   
